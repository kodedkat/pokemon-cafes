const jsonString = '{"name": "Pokémon Cafes", "version": 0.2.0}';

try {
    const parsedData = JSON.parse(jsonString);
    console.log(parsedData); // { name: 'Pokémon Cat Café', version: 1 }
} catch (error) {
    console.error("Parsing error:", error);
}